self-hosted-runner:
  labels: [psmux]
paths:
  .github/workflows/docs.yml:
    ignore:
      # GitHub supports queue; actionlint 1.7.12 does not yet recognize it.
      - '^unexpected key "queue" for "concurrency" section\. expected one of "cancel-in-progress", "group"$'
