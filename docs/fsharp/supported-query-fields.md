# F# portable query fields

`LibTmux.FSharp.Filter` creates version-one `QueryDocument` values. These are
the descriptors it exposes. `Query.matching` is local and materialized; it
does not send a native tmux filter.

The matrix is checked against documents generated by the F# descriptors and
the optional `LibTmux.Query.Json` serializer. `Pane.currentPath`, pane title,
geometry, parent links and placement data remain native captured-object
properties. They have no portable descriptor.

<!-- descriptor-matrix-start -->
| F# descriptor | Core property | Wire name | Value type | Operators | Required depth | Schema version |
| --- | --- | --- | --- | --- | --- | --- |
| SessionFields.name | Session.Name | session_name | string | eq, isNull, startsWith, oneOf | Sessions | 1 |
| SessionFields.id | Session.Id | session_id | SessionId | eq, oneOf | Sessions | 1 |
| SessionFields.attached | Session.Attached | session_attached | bool | eq, oneOf | Sessions | 1 |
| SessionFields.windows | Session.Windows | session_windows | Relation<Session, Window> | any, all, none | Windows | 1 |
| WindowFields.name | Window.Name | window_name | string | eq, isNull, startsWith, oneOf | Windows | 1 |
| WindowFields.id | Window.Id | window_id | WindowId | eq, oneOf | Windows | 1 |
| WindowFields.panes | Window.Panes | window_panes | Relation<Window, Pane> | any, all, none | Panes | 1 |
| PaneFields.currentCommand | Pane.CurrentCommand | pane_command | string | eq, isNull, startsWith, oneOf | Panes | 1 |
| PaneFields.id | Pane.Id | pane_id | PaneId | eq, oneOf | Panes | 1 |
| ClientFields.name | Client.Name | client_name | string | eq, isNull, startsWith, oneOf | Sessions | 1 |
| ClientFields.controlMode | Client.IsControlClient | client_control_mode | bool | eq, oneOf | Sessions | 1 |
<!-- descriptor-matrix-end -->

The core wire catalog also reserves `client_id`. The core client model has no
typed client ID, so the F# façade does not invent one. `session_windows` and
`window_panes` appear as typed relations; the façade does not expose a count
descriptor because its controlled translator input is the captured relation.

`allOf`, `anyOf` and `oneOf` reject empty inputs. Version one has no public
construction path for Boolean predicate identities, so the façade does not
claim that an empty conjunction or disjunction can be serialized.
