# tmux-workspace

Manage tmux workspaces from YAML or JSON using tmuxp-compatible commands.

The tool supports .NET 8 and .NET 10 on Unix. Python-specific shell and plugin behavior requires the optional tmuxp compatibility runtime. Native commands parse arguments and configuration before opening a tmux endpoint.
